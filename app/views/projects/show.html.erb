<p style="color: green"><%= notice %></p>

<%= render @project %>

<div>
  <%= link_to "Edit this project", edit_project_path(@project), :class => "btn btn-sm btn-primary" if user_signed_in?%> |
  <%= link_to "Back to projects", projects_path %>

  <%= button_to "Destroy this project", @project, method: :delete, :class => "btn btn-outline-danger" if user_signed_in?%> 
</div>
